<UserControl x:Class="FlatBase.FieldSnippets.fsnipOReferenceSingleton"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FlatBase"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
     TextElement.Foreground="{DynamicResource MaterialDesignBody}"
     TextElement.FontWeight="Regular"
     TextElement.FontSize="13"
     TextOptions.TextFormattingMode="Ideal"
     TextOptions.TextRenderingMode="Auto"
     Background="{DynamicResource MaterialDesignPaper}"
     FontFamily="{DynamicResource MaterialDesignFont}"
             HorizontalAlignment="Stretch">
    <Grid HorizontalAlignment="Stretch">
        <materialDesign:Card HorizontalAlignment="Stretch" Height="32" Name="Display">
            <Label Name="textDisplay" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,0,50,0">Text</Label>
        </materialDesign:Card>
        <Button Width="50"  Click="addItem" HorizontalAlignment="Right" Margin="100,0,0,0">
            <materialDesign:PackIcon Kind="CheckboxBlankCircleOutline"/>
        </Button>
    </Grid>
</UserControl>